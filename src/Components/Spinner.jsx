// Spinner.js
import React from 'react';
import './Spinner.css'; // Make sure to create this CSS file

const Spinner = () => {
    return (
        <div className="spinner">
            {/* You can also use an image here */}
        </div>
    );
};

export default Spinner;